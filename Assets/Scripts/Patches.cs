using HarmonyLib;

namespace ADOFAIUnityMod
{
    /// <summary>
    /// Add Harmony patches in this file. For example:
    /// [HarmonyPatch(typeof(SomeGameType), nameof(SomeGameType.SomeMethod))]
    /// internal static class SomeMethodPatch { ... }
    /// </summary>
    internal static class Patches
    {
    }
}
